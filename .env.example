# Server
PORT=4000
# Redis
REDIS_URL=redis://127.0.0.1:6379
# Supabase (use project URL and SERVICE_ROLE key) - SERVICE_ROLE needed for server actions
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
# Facebook
FB_APP_ID=your_fb_app_id
FB_APP_SECRET=your_fb_app_secret
# Prefer to use a system user token for server automation. If not present, store user tokens in DB.
FB_SYSTEM_ACCESS_TOKEN=
# Encryption key for tokens (32 bytes base64). Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
TOKEN_ENCRYPTION_KEY_BASE64=REPLACE_WITH_BASE64_32_BYTES
# Optional: PostgreSQL DB URL for local psql migration (not required if you use Supabase SQL editor)
SUPABASE_DB_URL=postgres://username:password@db-host:5432/postgres
